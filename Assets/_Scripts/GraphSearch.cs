using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Tilemaps;

public class GraphSearch
{
    /*
    public static Queue<Vector3Int> nodesToVisitInQueue = new Queue<Vector3Int>();

    public static BFSResult BFSGetRange(Tilemap map, Vector3Int startPoint, int movementPoints)
    {

        Dictionary<Vector3Int, Vector3Int> visitedNodes = new Dictionary<Vector3Int, Vector3Int>();
        Dictionary<Vector3Int, int> costOfVisitedNodes = new Dictionary<Vector3Int, int>();

        nodesToVisitInQueue.Enqueue(startPoint);
        costOfVisitedNodes.Add(startPoint, 0);
        visitedNodes.Add(startPoint, null);

        while (nodesToVisitInQueue.Count > 0)
        {
            foreach (var VARIABLE in COLLECTION)
            {

            }
        }

    }
    
    public static 
*/
}

public struct BFSResult
{

}
